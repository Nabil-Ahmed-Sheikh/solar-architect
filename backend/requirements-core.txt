# Core requirements - installs fast, no heavy geospatial dependencies
# Run: pip install -r requirements-core.txt
# This gets the full auth + projects + ROI + reports working immediately.
# Install requirements.txt separately for full LiDAR pipeline support.

Django==5.0.4
djangorestframework==3.15.1
django-cors-headers==4.3.1
djangorestframework-simplejwt==5.3.1
python-dotenv==1.0.1
requests==2.31.0
